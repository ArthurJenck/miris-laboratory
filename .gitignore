node_modules/
.next/
dist/
.env.local
.DS_Store
public/models/

# The budget-lab SDK ships with the repo. package.json installs core and three
# from vendor/, so a clone without them cannot npm install at all. The other two
# tarballs are 42MB that nothing here imports, so they stay out.
vendor/miris-inc-components-*.tgz
vendor/miris-web-sdk-*.tgz
.superpowers/
miris/data.json
miris/specimens.zip
# Offline rehearsal artefacts: the cube archive is rebuilt on every seed, and
# specimens-out/ is just the real zip unpacked for uploading to the portal.
miris/specimens.offline.zip
specimens-out/
.playwright-mcp/
tsconfig.tsbuildinfo

# Working directories, not part of the starter attendees clone: the raw kit
# drop, and the notes and specs written while building the guide.
miris-web-kit/
docs/
